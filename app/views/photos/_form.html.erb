<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Tytuł' %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label 'Plik' %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label 'Opis' %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label 'Dane techniczne' %><br>
    <%= f.text_area :technical_info %>
  </div>
  <div class="actions">
    <%= f.submit "Dodaj" %>
  </div>
<% end %>
